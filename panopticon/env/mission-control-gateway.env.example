# Mission Control gateway (nginx local env override)
# 说明：当前主路线由 mission-control-api 负责按 agent 注入 Authorization，
# 这里保留 TOKEN_* 变量模板，便于本地覆盖、排障和后续兼容扩展。

TOKEN_NOX=
TOKEN_METRICS=
TOKEN_EMAIL=
TOKEN_GROWTH=
TOKEN_TRADES=
TOKEN_HEALTH=
TOKEN_WRITING=
TOKEN_PERSONAL=
